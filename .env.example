# Supabase (local dev defaults — replace when migrating to cloud)
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.local-dev-placeholder-replace-with-output-of-supabase-start

# Demo mode — when true, the service layer skips Supabase and returns bundled mocks.
# Useful for presentations, screenshots, and offline development.
VITE_DEMO_MODE=false

# Legacy Puter AI — kept on for now. Will be removed when the Edge Function lands.
VITE_USE_PUTER_AI=true
